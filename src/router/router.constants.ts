/**
 * Query parameter name for passing action parameters in GET requests
 * @group Router
 */
export const PARAMS_QUERYNAME = 'q'

/**
 * Query parameter name for passing headers
 * @group Router
 */
export const HEADERS_QUERYNAME = 'h'

/**
 * HTTP header name for Content-Type
 * @group Router
 */
export const CONTENT_TYPE_HEADER = 'Content-Type'

/**
 * HTTP header name for Authorization
 * @group Router
 */
export const AUTHORIZATION_HEADER = 'Authorization'

/**
 * Content-Type value for multipart form data
 * @group Router
 */
export const MULTIPART_FORM_DATA = 'multipart/form-data'

/**
 * Content-Type value for JSON
 * @group Router
 */
export const APPLICATION_JSON = 'application/json'

/**
 * HTTP status code for success
 * @group Router
 */
export const HTTP_STATUS_OK = 200

/**
 * HTTP status code for not found
 * @group Router
 */
export const HTTP_STATUS_NOT_FOUND = 404
